import {
  Box,
  FormControl,
  InputLabel,
  MenuItem,
  Select,
  styled,
} from "@mui/material";

namespace S {
  export const ListContainer = styled(Box)(() => ({}));

  export const ListFormControl = styled(FormControl)(() => ({}));

  export const ListFormInputLabel = styled(InputLabel)(() => ({}));

  export const ListFormSelect = styled(Select)(() => ({}));

  export const ListFormMenuItem = styled(MenuItem)(() => ({}));
}

export default S;
